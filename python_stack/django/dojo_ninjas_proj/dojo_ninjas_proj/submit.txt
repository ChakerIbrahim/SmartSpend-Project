from dojo_ninjas_app.models import Dojo, Ninja

# Create 3 new dojos

Dojo.objects.create(name="Python Stack", city="London", state="EN")
Dojo.objects.create(name="Java Stack", city="Ramallah", state="PS")
Dojo.objects.create(name="MERN Stack", city="Paris", state="FR")

# Delete the 3 dojos you just created

Dojo.objects.all().delete()

# Create 3 more dojos

dojo1 = Dojo.objects.create(name="Python", city="Palestine", state="PS")
dojo2 = Dojo.objects.create(name="JAVA", city="Palestine", state="PS")
dojo3 = Dojo.objects.create(name="MERN", city="Palestine", state="PS")

# Create 3 ninjas that belong to the first dojo

Ninja.objects.create(first_name="JALIL", last_name="WASAYA", dojo=dojo1)
Ninja.objects.create(first_name="Ahmad", last_name="Saleh", dojo=dojo1)
Ninja.objects.create(first_name="Murad", last_name="Shaheen", dojo=dojo1)

# Create 3 ninjas that belong to the second dojo

Ninja.objects.create(first_name="Nasri", last_name="Ladaa", dojo=dojo2)
Ninja.objects.create(first_name="Chaker", last_name="Saleh", dojo=dojo2)
Ninja.objects.create(first_name="Dana", last_name="Salah", dojo=dojo2)

# Create 3 ninjas that belong to the third dojo

Ninja.objects.create(first_name="Selina", last_name="Hanhan", dojo=dojo3)
Ninja.objects.create(first_name="Lina", last_name="Saboubi", dojo=dojo3)
Ninja.objects.create(first_name="Mira", last_name="Martha", dojo=dojo3)

# Retrieve all the ninjas from the first dojo

Dojo.objects.first().ninjas.all()

# Retrieve all the ninjas from the last dojo

Dojo.objects.last().ninjas.all()

# Retrieve the last ninja's dojo

Ninja.objects.last().dojo

# After adding desc field and running migrations

Dojo.objects.create(
name="FullStack",
city="Ramallah",
state="PS",
desc="This is a new dojo"
)
